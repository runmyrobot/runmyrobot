echo This will create an entry in known hosts for root when it tries to connect so that ssh can be run from cron. Note: It doesn't matter that it won't actually connect without the pem specified. It will still created the neccesary entry.
sudo -u root ssh rssh@34.216.85.34
